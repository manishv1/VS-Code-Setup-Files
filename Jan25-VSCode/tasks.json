{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "echo ManishV Compiling ${fileBasename} using c++17... && g++ --std=c++17 -g ${file} -o ${fileDirname}/${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build task that compiles C++ files."
        }
    ]
}
